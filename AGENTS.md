# COMP3014 网络模拟项目知识整理

## 1. 项目概述与小组工作指南

### 1.1 项目基础信息
- **课程代码**: COMP3014
- **项目占比**: 30%的最终成绩
- **小组规模**: 3名学生
- **评分公式**:
  ```
  StudentA Score = (∑Score for StudentA tasks / ∑Scores of all tasks) × Total mark of the project(0-100) × Number of students
  ```

### 1.2 任务管理要求
- 将项目分解为多个小任务（既不能太大也不能太小）
- 每个任务需分配:
  - 分数(0-10)
  - 截止日期
  - 负责人
- 每个学生的任务总分应该相等
- 未按时完成的惩罚: 该学生失去该任务分数，其他学生可接手并获得原分数的一半

### 1.3 工具建议
- **Trello**: 推荐用于协作管理任务
  - 创建项目管理看板
  - 添加小组成员
  - 为每个任务创建卡片并分配负责人

### 1.4 重要截止日期
- **小组合同和初步任务/分数表**: 2025年9月30日（PDF格式）
- **Trello看板设置**: 2025年10月21日（PDF和链接）
- **最终小组报告**: 2025年11月21日（最多10页，包含任务/分数表）

## 2. 项目技术内容 - TCP变体分析

### 2.1 项目结构
项目分为三个部分(A、B、C)，主要关注不同TCP拥塞控制算法的性能比较。

### 2.2 模拟设置
- **TCP变体**: Cubic, Reno, Yeah, Vegas
- **运行命令**:
  ```bash
  cd comp3014j
  ns cubicCode.tcl; ns yeahCode.tcl; ns renoCode.tcl; ns vegasCode.tcl
  ```
- **分析工具**: 需要编写Python脚本分析结果

### 2.3 Part A (40分): TCP变体，固定拓扑
1. **任务1 (10分)**: 创建表格展示每流总吞吐量(Goodput Mbps)和丢包率(PLR pct)，并添加比较图表(两个子图: goodput, PLR)
2. **任务2 (10分)**: 计算最后三分之一期间的Jain公平性指数，分析哪个变体最公平
3. **任务3 (10分)**: 分析吞吐量稳定性(最低变异系数CoV)，解释与算法增长/退避逻辑的关系
4. **任务4 (10分)**: 总结在此设置下哪个算法"最好"及原因(3-5句话)

#### Jain公平性指数公式
```
J(x₁, x₂,..., xₙ) = (∑ⁿᵢ₌₁ xᵢ)² / (n × ∑ⁿᵢ₌₁ xᵢ²)
```
- 范围: 0(最不公平)到1(完全公平)
- xᵢ: 流i的吞吐量(或资源份额)
- n: 总流数

### 2.4 队列管理算法
1. **Drop Tail**:
   - 当队列达到最大容量时，丢弃新到达的数据包
   - 导致TCP发送方进入慢启动状态，降低吞吐量

2. **Random Early Detection (RED)**:
   - 当平均队列大小超过最小阈值时，以非零概率丢弃数据包
   - 不等待队列完全填满
   - 目标: 提供高链路利用率、消除对突发源的偏见、减少丢包和排队延迟

### 2.5 Part B (35分): DropTail vs RED
1. **任务1 (15分)**: 比较DropTail和RED在goodput、PLR、公平性、稳定性(CoV)方面的差异
2. **任务2 (20分)**: 敏感性分析(额外一个容量) - 分析拥塞级别如何影响队列算法选择(150-250字)

### 2.6 Part C (25分): 轻量级可重复性
1. **任务1 (15分)**: 选择一个有趣场景，用不同种子/开始时间重复5次运行，展示平均值和95%置信区间
2. **任务2 (10分)**: 创建单个shell脚本，可(i)运行模拟，(ii)运行分析器，(iii)重新生成CSV和图表

## 3. 统计分析基础

### 3.1 样本与总体
- **总体参数**: 用希腊字母表示(μ, ρ)
- **样本统计**: 用罗马字母表示(x̄, s)
- 样本统计是对总体参数的估计

### 3.2 置信区间
- **定义**: 一个值范围，我们确信真实值位于其中
- **公式**: 
  ```
  X̄ ± Z × (s / √n)
  ```
  - X̄: 样本均值
  - Z: 根据置信水平确定(Z值表)
  - s: 样本标准差
  - n: 样本大小

#### 常用Z值表
| 置信水平 | Z值   |
|---------|-------|
| 80%     | 1.282 |
| 85%     | 1.440 |
| 90%     | 1.645 |
| 95%     | 1.960 |
| 99%     | 2.576 |
| 99.5%   | 2.807 |
| 99.9%   | 3.291 |

### 3.3 中心极限定理
- **条件**: 观察x₁, x₂,..., xₙ是独立的，来自同一总体(均值μ，标准差ρ)
- **结论**: 大样本的样本均值x̄近似服从正态分布 N(μ, ρ/√n)
- **标准误差**: ρ/√n (样本均值的标准差)

### 3.4 确定适当样本大小
- 样本量越大，置信度越高
- 目标: 确定满足所需精度的最小样本量
- 与实验成本和错误决策的潜在损失相关

### 3.5 网络模拟中的统计应用
- 模拟结果通常具有随机性
- 需要多次运行以获得可靠结果
- 置信区间用于量化结果的可靠性
- 例如: 对1000个数据包大小进行抽样，计算99%置信区间:
  ```
  40 ± 2.576 × (5/√1000) = 40 bytes ± 0.41 bytes
  ```

## 4. 项目交付物总结

1. **小组合同与任务分配** (9月30日)
   - 任务分解表
   - 分数分配
   - 小组工作规则

2. **Trello项目管理** (10月21日)
   - 任务看板设置
   - 进度跟踪

3. **最终报告** (11月21日)
   - 最多10页
   - 详细描述项目实现(Part A, B)
   - 回答Part C的问题
   - 任务/分数表
   - 分析图表与结论

4. **技术交付物**
   - analyser3.py (Python分析脚本)
   - 模拟结果图表
   - 置信区间计算
   - 自动化shell脚本
   